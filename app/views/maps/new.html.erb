<div id="messages">
	<span class="welcome_box background_grey">
		okay step 1: double click on the map to add a point
	</span>
	<br />
	<span class="welcome_box background_orange italics">
		yup, step 2: you're pretty much done
	</span>
	<br />
	<span class="welcome_box background_orange italics">
		baaaaaaaaam: describe the points & save
	</span>
</div>
<script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"> </script>
<script type="text/javascript" >
  var data_set = null;
</script>
<div class="container">
  <div id="new_map_details" class="left">
	<%= javascript_include_tag 'map' %>
	<%= form_for(@map) do |f| %>
	  <% if @map.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@map.errors.count, "error") %> prohibited this map from being saved:</h2>

	      <ul>
	      <% @map.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

	  <div class="field">
	    <%= f.label :name, 'what is this map about?' %>
	    <br />
	    <%= f.text_field :name %>
	  </div>
	  <div id="points_scroller">
	      <ul id="points">
	      </ul>
	  </div>
	  <div class="actions">
	    <%= f.submit 'Save' %> or <%= link_to 'Cancel', maps_path %>
	  </div>
	<% end %>
  </div>
  <div class="map_points right">
    <label for='address'>Go to a location:</label> <input id="address" type="textbox" value="Sunnyvale, CA"> <input type="button" value="Whisk me away" onclick="codeAddress()">
    <div id="map_canvas"></div>
  </div>
</div>
<div style="clear:both"></div>